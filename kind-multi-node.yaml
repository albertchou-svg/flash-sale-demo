kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  # 👑 這是 Control Plane (Master)
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"

  # 👷 這是 Worker 1 (負責跑 App)
  - role: worker
    extraPortMappings:
      - containerPort: 30000 # 預留給 NodePort 用
        hostPort: 80         # 對應到您電腦的 localhost:80

  # 👷 這是 Worker 2 (負責跑 App)
  - role: worker